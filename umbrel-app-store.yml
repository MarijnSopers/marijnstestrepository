id: "sparkles"        # Your app store ID (used as prefix for all app IDs)
name: "marijn"        # The store name (shows up as "marijn App Store")
apps:
  - sparkles-proxmox-dashboard  # Must match folder and app ID
